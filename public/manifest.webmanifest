{
  "name": "mdread — markdown reader",
  "short_name": "mdread",
  "description": "A local-first, privacy-first markdown reader. Drop a file or folder, read beautifully, edit, and download — all on your device. Sharing is end-to-end encrypted: the key stays in the link, so the server can't read it.",
  "start_url": "./",
  "scope": "./",
  "display": "standalone",
  "background_color": "#faf6ee",
  "theme_color": "#faf6ee",
  "icons": [
    { "src": "./icons/icon.svg", "type": "image/svg+xml", "sizes": "any", "purpose": "any maskable" }
  ]
}
